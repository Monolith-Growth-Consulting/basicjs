// taking user inputs

// you are manipulating two objects here: the window object (browser) and the console object
var name = window.prompt("Enter your name: ")

// outputting the name
console.log(`this is your name: ${name}`);