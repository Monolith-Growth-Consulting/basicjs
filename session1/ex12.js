// taking user inputs
var name = window.prompt("Enter your name: ")
var weight = window.prompt("Enter your weight in pounds: ")

// outputting the name
console.log(`name: ${name} | weight: ${weight}`);