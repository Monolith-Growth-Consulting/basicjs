// string interpolation (pa rin)

// thus we're just re-using ex4 (we're following the path of the python book)

var num1 = 2;
var num2 = 2;

var sum = num1 + num2;
var product = num1 * num2;
var quotient = num1 / num2;

// bare console log
console.log("sum: ");


// string interpolation version
console.log(`sum: ${sum}`);
console.log(`product: ${product}`);
console.log(`quotient: ${quotient}`);